version: '3.8'

services:
  temporal-graph-rag:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3007:3007"
    environment:
      - PORT=3007
      - NODE_ENV=production
    restart: unless-stopped
